# xAI API Key (Required)
# Get your API key from: https://console.x.ai/
XAI_API_KEY=xai-your-api-key-here

# Optional: Enable debug logging
# DEBUG=true

# Optional: Default output directory for generated videos
# OUTPUT_DIR=./output

# Optional: Default polling interval in milliseconds (default: 5000)
# VIDEO_POLL_INTERVAL=5000

# Optional: Default max polling attempts (default: 120)
# VIDEO_MAX_POLL_ATTEMPTS=120

# Cloudflare R2 Configuration (Required for image_path feature)
# Used to automatically upload local images for image-to-video generation
# Get credentials from: Cloudflare Dashboard > R2 > Manage R2 API Tokens
# R2_ACCOUNT_ID=your-account-id
# R2_ACCESS_KEY_ID=your-access-key-id
# R2_SECRET_ACCESS_KEY=your-secret-access-key
# R2_BUCKET_NAME=your-bucket-name
# R2_PUBLIC_URL=https://pub-xxxx.r2.dev
